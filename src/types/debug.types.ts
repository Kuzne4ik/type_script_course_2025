/*
import type { ComponentProps } from "react"
import type { IPlayerState } from "./player.type"

// Если не установлено расширение для чтения полей в всплывающие подсказки то можно использовать перечитку в тип
type Debug<T> = { [K in keyof T]: T[K]}
// Пример: перчитка в тип 
type CheckType = Debug<IPlayerState>;


// Если не установлено расширение для чтения полей в всплывающие подсказки то можно использовать перечитку в тип
type ButtonProps = ComponentProps<'button'>;
*/

